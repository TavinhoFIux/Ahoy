@using Application.NoticiaCommands.Commands

@model CreateNoticiaCommand

<h2>Create Notícia</h2>

<form asp-controller="Noticia" asp-action="Create" method="post" id="noticiaForm">
    <div class="form-group">
        <label asp-for="Titulo" class="control-label">Título:</label>
        <input asp-for="Titulo" class="form-control" placeholder="Digite o título da notícia" />
        <span asp-validation-for="Titulo" class="text-danger"></span>
    </div>
    <div class="form-group">
        <label asp-for="Texto" class="control-label">Texto:</label>
        <textarea asp-for="Texto" class="form-control" rows="5" placeholder="Digite o texto da notícia"></textarea>
        <span asp-validation-for="Texto" class="text-danger"></span>
    </div>
    <div class="form-group">
        <label asp-for="TagIds" class="control-label">Tags:</label>
        <select asp-for="TagIds" asp-items="ViewBag.Tags" class="form-control" multiple="multiple"></select>
    </div>
    <button type="submit" class="btn btn-success">Salvar</button>
    @Html.AntiForgeryToken()
</form>

<a asp-action="Index" class="btn btn-default">Voltar à Lista</a>